import 'package:taboo/tabs/taboo_tab.dart';

/// A static tab is a tab that has only static content. An example of this
/// would be the [WelcomeTab]. Static tabs can't be saved to files
/// but they can be closed.
abstract class StaticTab extends TabooTab {

}